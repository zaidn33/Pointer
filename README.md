# Pointer

Pointer is a privacy-first credit card rewards advisor. Users save the cards they already have, then ask which card to use for a merchant or category.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to run the app locally.

## Verification

```bash
npm run lint
npm run build
npx prisma validate
```
